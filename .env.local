# .env.local — только для локальной разработки (go run / IDE)
# НЕ КОММИТЬ В GIT!

# В локальной среде база данных доступна по localhost
DB_HOST=localhost

# Опционально: если у тебя локальная БД на другом порту
# DB_PORT=5433